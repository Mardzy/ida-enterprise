```js
<section>
  <Container whitespaceAfter={2}>
    <p>Container with 2 units of whitespace after the DOM node.</p>
  </Container>
  <Container whitespaceAfter={2}>
    <p>No whitespace after, as it is the last child of its container.</p>
  </Container>
</section>
```